# Cover Page Samples

This folder contains compiled PDF previews for the CAU thesis title-page variants.
The LaTeX source files live in `latex_sources/` and can be rebuilt with:

```sh
cd examples/cover_page_samples/latex_sources
./compile_cover_pages.sh
```