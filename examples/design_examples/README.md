# Design Examples

This folder contains full-document PDF previews using different CAU thesis cover settings.
The LaTeX source files live in `latex_sources/` and can be rebuilt with:

```sh
cd examples/design_examples/latex_sources
./compile_design_examples.sh
```